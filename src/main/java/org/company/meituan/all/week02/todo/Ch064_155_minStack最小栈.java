package org.company.meituan.all.week02.todo;

/**
 *<p>155. 最小栈
 *<p>设计一个支持 push ，pop ，top 操作，并能在常数时间内检索到最小元素的栈。
 *<p><p>
 *<p>push(x) —— 将元素 x 推入栈中。
 *<p>pop() —— 删除栈顶的元素。
 *<p>top() —— 获取栈顶元素。
 *<p>getMin() —— 检索栈中的最小元素。
 *<p><p>
 *<p><p>
 *<p>示例:
 *<p><p>
 *<p>输入：
 *<p>["MinStack","push","push","push","getMin","pop","top","getMin"]
 *<p>[[],[-2],[0],[-3],[],[],[],[]]
 *<p><p>
 *<p>输出：
 *<p>[null,null,null,null,-3,null,0,-2]
 *<p><p>
 *<p>解释：
 *<p>MinStack minStack = new MinStack();
 *<p>minStack.push(-2);
 *<p>minStack.push(0);
 *<p>minStack.push(-3);
 *<p>minStack.getMin();   --> 返回 -3.
 *<p>minStack.pop();
 *<p>minStack.top();      --> 返回 0.
 *<p>minStack.getMin();   --> 返回 -2.
 *<p><p>
 *<p><p>
 *<p>提示：
 *<p><p>
 *<p>pop、top 和 getMin 操作总是在 非空栈 上调用。
 *<p>通过次数237,533提交次数418,775
 *<p>请问您在哪类招聘中遇到此题？
 *<p>https://leetcode-cn.com/problems/min-stack/
 *
 *<p>@Author: shenpei
 *<p>@Date: 2021/5/17 6:24 下午
 */
public class Ch064_155_minStack最小栈 {
    /**
     *<p>initialize your data structure here.
     */
    public Ch064_155_minStack最小栈() {

    }

    public void push(int val) {

    }

    public void pop() {

    }

    public int top() {
        return -1;
    }

    public int getMin() {
        return -1;
    }
}
